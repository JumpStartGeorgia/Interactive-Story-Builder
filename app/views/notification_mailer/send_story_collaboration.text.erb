<%= @message.message %>

<% @message.message_list.each do |msg| %>
<%= "#{t('mailer.notification.story_collaboration.from', :locale => @message.locale)}: #{msg[0]}" %>
<%= "#{t('mailer.notification.story_collaboration.story_title', :locale => @message.locale)}: #{msg[1]}" %>
<% if msg[3].present? %>
<%= msg[3] %>
<% end %>

<%= "#{t('mailer.notification.story_collaboration.accept_link', :locale => @message.locale)}: #{settings_accept_invitation_url(:locale => @message.locale, :key => msg[2])}" %>

------------------

<% end %>



<%= "#{t('mailer.notification.story_collaboration.all_invitations', :locale => @message.locale)}: #{settings_invitations_url(:locale => @message.locale)}" %>

