<%= simple_format @message.message %>

<ul>
<% @message.message_list.each do |msg| %>
  <li>
    <%= link_to msg[0], theme_url(:id => msg[1], :locale => @message.locale) %>
  </li>
<% end %>
</ul>

  
