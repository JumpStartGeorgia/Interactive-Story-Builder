<%= simple_format @message.message %>

<ul>
<% @message.message_list.each do |msg| %>
  <li>
    <%= msg[2] %> - 
    <%= link_to msg[0], storyteller_show_url(:id => msg[1], :locale => @message.locale) %>
  </li>
<% end %>
</ul>

  
