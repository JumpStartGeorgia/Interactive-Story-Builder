 <div class='info-top'>
   <div class="share">
       <div class="addthis_native_toolbox"></div> 
       <%= link_to '#', :'data-modalos-id' => "modalos-embed", :title => t('app.common.embed'), :class => "embed modalEmbed" do %>

         <% end %> 
   </div>
 <div class="views" title=""><%= t('app.common.views') %>:&nbsp;<span><%= @story.impressions_count %></span></div>  
 <div class="r">  
   <%  
     locales = @story.story_locales
     cur_locale = @story.current_locale 
     cur_lang = @languages.select{ |x| x.locale == cur_locale }.first
     locales-=[cur_locale]
   %>
     <div class='translations'>
       <div><%= t('helpers.links.languages') %>:&nbsp;</div>
       <div class="dropdown">
         <button class='btn btn-default dropdown-toggle<%= locales.size > 0 ? "" : " disabled" %>' type="button" id="translationsMenu" data-toggle="dropdown" aria-expanded="true">
           <%= cur_lang.name %>
             <span class="caret"></span>                               
         </button>
         <% if locales.size > 0 %>
           <ul class="dropdown-menu" role="menu" aria-labelledby="translationsMenu">
             <%  locales.each do |t| 
                   @story.current_locale = t
                   l = @languages.select{|x| x.locale == t}.first
                   if l.present?
             %>
               <li role="presentation"><a role="menuitem" tabindex="-1" href="<%= storyteller_show_path(:id=>@story.permalink,:sl =>t) %> "><%= l.name %></a></li>
               <% end %>
             <% end %>
           </ul>
         <% end %>
       </div>
     </div>
   <% 
     @story.current_locale = cur_locale
   %>
   <a href="#" class="navigation-volume"></a>
 </div>
</div>