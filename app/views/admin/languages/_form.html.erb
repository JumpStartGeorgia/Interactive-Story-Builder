<%= semantic_form_for([:admin,@language]) do |f| %>
	<%= f.error_messages %>

	<%= f.inputs do %>
		<%= f.input :locale %>
		<%= f.input :name %>
	<% end %>


  <div>
    <%= f.submit nil, :class => 'btn btn-primary' %>
	  <%= f.submit t('helpers.links.reset'), :class => 'btn btn-warning', :type => :reset, :name => :reset %>
	  <%= link_to t('helpers.links.cancel'), admin_languages_path, :class => 'btn btn-xs btn-warning' %>
  </div>

<% end %>

