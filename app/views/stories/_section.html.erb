<%= semantic_form_for(@item, url: section_story_path(id:params[:id]), remote: true) do |f| %>

  <%= f.inputs do %>
    <%= f.input :title, :placeholder => 'Type section title', :input_html => { :id => "sectionTitle" } %>
     <% if (@item.type_id == 1 && !@item.content.present? ) || (@item.type_id == 2 && !@item.media.present? ) %>
    	<%= f.input :type_id, :as => :select,  :include_blank => false, :collection => { 'Content' => 1, 'Media' => 2 } , :default => 0 , :input_html => { :id => "sectionType" }   %>  
    <% end %>
    <%= f.input :has_marker, :as => :boolean, :checked_value => 1, :unchecked_value => 0 %>   
    <% if @item.id.present? %>
      <%= f.input :id, :as => :hidden , :input_html => { :id => "sectionId" }  %>    
    <% end %>
  <% end %>
  <div class="pull-right spacer">
    <%= f.submit nil, :class => 'btn' %>
    <%= f.submit nil, :class => 'btn', :type => :reset, :value => t('helpers.links.reset'), :name => :reset %>      
  </div>
<% end %>

