 $(document).ready(function() 
 {
	if(item_id != -1)
	{
		$('.story-tree ul li.item[id='+ section_id + ']').find(' ul li.sub[id='+item_id+']').remove();
	}
	else 
	{	
		$('.story-tree ul li.item[id='+ section_id + ']').remove();
	}
	$("html, body").animate({ scrollTop: 0 }, "slow");
});