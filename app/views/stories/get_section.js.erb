<% if @which == 0 || @which == 1 %>
   which(1,"<%= escape_javascript( render :partial => 'section', locals: {original: true}) %>");
<% end %>
<% if @trans && (@which == 0 || @which == 2) %>
   <% @item.current_locale = @to %>
   which(2,"<%= escape_javascript( render :partial => 'section') %>");
<% end %>