

$("#storyViewer").hide().html("<div id='storyForm'><%= escape_javascript( render :partial => 'form', locals: {original: true}) %></div>").fadeIn('slow');
<% if @trans %>
	$(".form2").hide().html("<div id='storyForm'><%= escape_javascript( render :partial => 'form') %></div>").fadeIn('slow');
<% end %>
<% imageTemp = '' %>
<% if @story.asset.asset.exists? %>
  //<%  imageTemp = "<img  width='100%' src='#{@story.asset.asset.url}'/>" %>
  $('#story-thumbnail-preview').attr('data-image-path','<%= "#{@story.asset.asset.url}" %>');
<% end %>

//$("#modalImage .modal-data").html("<%= imageTemp.html_safe %>");
